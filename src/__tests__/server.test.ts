// Basic dummy test file
describe('Server', () => {
  it('should pass a dummy test', () => {
    expect(true).toBe(true);
  });

  it('should correctly add two numbers', () => {
    expect(1 + 1).toBe(2);
  });
}); 